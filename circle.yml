machine:
  node:
    version: 8.0
  services:
    - docker

deployment:
  production:
    branch: production
    commands:
      - gulp gcloudDeploy
  staging:
    branch: staging
    commands:
      - gulp gcloudDeploy --env=staging
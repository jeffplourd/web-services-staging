machine:
  environment:
    PROJECT_NAME: web-services-staging
    CLUSTER_NAME: web-services
    CLOUDSDK_COMPUTE_ZONE: us-central1-a
  node:
    version: 8.0
  services:
    - docker

dependencies:
  post:
    - gulp gcloudUpdate
    - gulp kubeUpdate
    - gulp createServiceKeyFromEncryption
    - gulp gcloudAuthServiceAccount
#    - echo $GCLOUD_SERVICE_KEY | base64 --decode -i > ${HOME}/gcloud-service-key.json
#    - sudo /opt/google-cloud-sdk/bin/gcloud auth activate-service-account --key-file ${HOME}/gcloud-service-key.json
    - sudo /opt/google-cloud-sdk/bin/gcloud config set project $PROJECT_NAME
    - sudo /opt/google-cloud-sdk/bin/gcloud --quiet config set container/cluster $CLUSTER_NAME
    - sudo /opt/google-cloud-sdk/bin/gcloud config set compute/zone ${CLOUDSDK_COMPUTE_ZONE}
    - sudo /opt/google-cloud-sdk/bin/gcloud --quiet container clusters get-credentials $CLUSTER_NAME

#    - docker build -t us.gcr.io/${PROJECT_NAME}/hello:$CIRCLE_SHA1 -f dockerfiles/Dockerfile.${CIRCLE_BRANCH} .
#    - docker tag us.gcr.io/${PROJECT_NAME}/hello:$CIRCLE_SHA1 us.gcr.io/${PROJECT_NAME}/hello:latest
#    - ls -a
    - gulp testTask

deployment:
  production:
    branch: production
    commands:
#      - bash deploy.sh
  staging:
    branch: staging
    commands:
#      - bash deploy.sh